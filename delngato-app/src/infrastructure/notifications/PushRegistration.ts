/**
 * usePushRegistration — DISABLED for prototype mode.
 *
 * Push notification registration is fully stubbed out to prevent unstable
 * startup in the prototype. Will be re-enabled for production backend integration.
 */

export function usePushRegistration(): void {
  // Prototype mode: push registration disabled entirely.
}
